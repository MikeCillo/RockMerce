<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.example</groupId>
    <artifactId>RockMerce</artifactId>
    <version>1.0-SNAPSHOT</version>
    <name>RockMerce</name>
    <packaging>war</packaging>

    <properties>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <maven.compiler.target>1.8</maven.compiler.target>
        <maven.compiler.source>1.8</maven.compiler.source>
        <junit.version>5.8.2</junit.version>
        <!-- Esclusione Sonar per cartella JMH -->
        <sonar.exclusions>**/src/jmh/**/*.*</sonar.exclusions>
        <!-- Esclude i sorgenti LogicTier dalla coverage report -->
        <sonar.coverage.exclusions>
            **/DataTier/DBCONNECTION/DbConnection.java,
            **/LogicTier/GestioneAdmin/**/*.java,
            **/LogicTier/GestioneAutenticazione/**/*.java,
            **/LogicTier/GestioneCart/**/*.java,
            **/LogicTier/GestioneGuitars/**/*.java,
            **/LogicTier/GestioneSignUp/**/*.java,
            **/LogicTier/AboutUsServlet.java,
            **/LogicTier/BackHomepageServlet.java,
            **/LogicTier/RockMerceStartServlet.java,
            **/Scripts/AdminJS/UpdateGuitarValidation.js,
            **/Scripts/SignUpJS/SignUpValidationJS.js,
            **/Scripts/accessibilita.js
        </sonar.coverage.exclusions>
    </properties>

    <dependencies>
        <dependency>
            <groupId>org.mockito</groupId>
            <artifactId>mockito-core</artifactId>
            <version>4.9.0</version>
            <scope>test</scope>
        </dependency>

        <dependency>
        <groupId>com.google.protobuf</groupId>
        <artifactId>protobuf-java</artifactId>
        <version>4.33.1</version>
        </dependency>

        <!-- H2 in-memory DB for tests -->
        <dependency>
            <groupId>com.h2database</groupId>
            <artifactId>h2</artifactId>
            <version>2.2.224</version>
            <scope>test</scope>
        </dependency>


        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-core</artifactId>
            <version>1.37</version>
        </dependency>

        <dependency>
            <groupId>org.openjdk.jmh</groupId>
            <artifactId>jmh-generator-annprocess</artifactId>
            <version>1.37</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>junit</groupId>
            <artifactId>junit</artifactId>
            <version>4.13.2</version>
            <scope>test</scope>
        </dependency>

        <dependency>
            <groupId>mysql</groupId>
            <artifactId>mysql-connector-java</artifactId>
            <version>8.0.28</version>
        </dependency>

        <dependency>
            <groupId>org.apache.tomcat</groupId>
            <artifactId>tomcat-jdbc</artifactId>
            <version>10.0.17</version>
        </dependency>


        <dependency>
            <groupId>org.glassfish.web</groupId>
            <artifactId>jakarta.servlet.jsp.jstl</artifactId>
            <version>2.0.0</version>
        </dependency>

        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>5.0.0</version>
            <scope>provided</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-api</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.junit.jupiter</groupId>
            <artifactId>junit-jupiter-engine</artifactId>
            <version>${junit.version}</version>
            <scope>test</scope>
        </dependency>
        <dependency>
            <groupId>org.testng</groupId>
            <artifactId>testng</artifactId>
            <version>7.11.0</version>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <build>

        <plugins>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>build-helper-maven-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <execution>
                        <id>add-jmh-sources</id>
                        <phase>generate-sources</phase>
                        <goals>
                            <goal>add-source</goal>
                        </goals>
                        <configuration>
                            <sources>
                                <source>src/jmh</source>
                            </sources>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-shade-plugin</artifactId>
                <version>3.5.1</version>
                <executions>
                <execution>
                    <phase>package</phase>
                    <goals>
                        <goal>shade</goal>
                    </goals>
                    <configuration>
                        <shadedArtifactAttached>true</shadedArtifactAttached>
                        <shadedClassifierName>benchmarks</shadedClassifierName>
                        <finalName>benchmarks</finalName>
                        <transformers>
                            <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                <mainClass>org.openjdk.jmh.Main</mainClass>
                            </transformer>
                        </transformers>
                    </configuration>
                </execution>
            </executions>
            </plugin>

            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-war-plugin</artifactId>
                <version>3.3.2</version>
            </plugin>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.10.1</version>
                <configuration>
                    <!-- usa le proprietà già definite per coerenza -->
                    <source>9</source>
                    <target>9</target>
                    <encoding>${project.build.sourceEncoding}</encoding>
                    <!-- directory dove maven mette le sorgenti generate dall'annotation processor -->
                    <generatedSourcesDirectory>${project.build.directory}/generated-sources/annotations</generatedSourcesDirectory>
                    <annotationProcessorPaths>
                        <path>
                            <groupId>org.openjdk.jmh</groupId>
                            <artifactId>jmh-generator-annprocess</artifactId>
                            <version>1.37</version>
                        </path>
                    </annotationProcessorPaths>
                </configuration>
            </plugin>

            <!-- Force a modern surefire so JUnit4 tests are executed reliably -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-surefire-plugin</artifactId>
                <version>3.1.2</version>
            </plugin>


            <plugin>
                <groupId>org.jacoco</groupId>
                <artifactId>jacoco-maven-plugin</artifactId>
                <version>0.8.14</version>
                <executions>
                    <execution>
                        <id>prepare-agent</id>
                        <goals>
                            <goal>prepare-agent</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>report</id>
                        <phase>verify</phase>
                        <goals>
                            <goal>report</goal>
                        </goals>
                        <configuration>
                            <!-- Genera il report XML nella posizione che SonarQube si aspetta per default -->
                            <dataFile>${project.build.directory}/jacoco.exec</dataFile>
                            <!-- output sia nella cartella `target/site/jacoco` (default Sonar) sia in `target/jacoco-report` per compatibilità -->
                            <outputDirectory>${project.reporting.outputDirectory}/jacoco</outputDirectory>
                            <formats>
                                <format>XML</format>
                                <format>HTML</format>
                            </formats>
                            <!-- Esclude i benchmark/jmh dalla reportistica di JaCoCo -->
                            <excludes>
                                <!-- exclude class files generated under any 'benchmarks' package -->
                                <exclude>**/benchmarks/**</exclude>
                                <!-- exclude any class with Benchmark in the name -->
                                <exclude>**/*Benchmark*.class</exclude>
                                <!-- exclude jmh source folder classes if compiled under jmh package -->
                                <exclude>**/jmh/**</exclude>
                            </excludes>
                        </configuration>
                    </execution>
                </executions>
            </plugin>

            <!-- Aggiunto plugin per eseguire test JMeter -->
            <plugin>
                <groupId>com.lazerycode.jmeter</groupId>
                <artifactId>jmeter-maven-plugin</artifactId>
                <version>3.3.0</version>
                <executions>
                    <!-- Execution che crea il file di configurazione necessario (config.json) -->
                    <execution>
                        <id>configuration</id>
                        <phase>compile</phase>
                        <goals>
                            <goal>configure</goal>
                        </goals>
                    </execution>
                    <execution>
                        <id>jmeter-tests</id>
                        <!-- Il goal 'jmeter' va legato alla fase integration-test -->
                        <phase>integration-test</phase>
                        <goals>
                            <goal>jmeter</goal>
                        </goals>
                        <configuration>
                            <!-- cartella dove mettere i .jmx (committare i .jmx in VCS sotto src/test/jmeter) -->
                            <testFilesDirectory>src/test/jmeter</testFilesDirectory>
                            <resultsDirectory>${project.build.directory}/jmeter-results</resultsDirectory>
                            <!-- abilita la generazione dei report HTML da parte di JMeter dopo l'esecuzione -->
                            <generateReports>true</generateReports>
                            <reportDirectory>${project.build.directory}/jmeter-reports</reportDirectory>
                        </configuration>
                    </execution>
                    <execution>
                        <id>jmeter-results</id>
                        <!-- Analizza i risultati e decide l'esito del build -->
                        <phase>verify</phase>
                        <goals>
                            <goal>results</goal>
                        </goals>
                        <configuration>
                            <resultsDirectory>${project.build.directory}/jmeter-results</resultsDirectory>
                            <reportDirectory>${project.build.directory}/jmeter-reports</reportDirectory>
                            <!-- Abilita la scansione dei risultati per richieste fallite -->
                            <scanResultsForFailedRequests>true</scanResultsForFailedRequests>
                            <scanResultsForSuccessfulRequests>true</scanResultsForSuccessfulRequests>
                            <!-- Se ci sono richieste fallite, il build fallisce (ignoreResultFailures=false) -->
                            <ignoreResultFailures>false</ignoreResultFailures>
                        </configuration>
                    </execution>
                </executions>
                <configuration>
                    <!-- configurazione globale del plugin (valori di default sovrascritti nelle singole execution) -->
                    <testFilesDirectory>src/test/jmeter</testFilesDirectory>
                    <resultsDirectory>${project.build.directory}/jmeter-results</resultsDirectory>
                    <reportDirectory>${project.build.directory}/jmeter-reports</reportDirectory>
                    <jmeterVersion>5.5</jmeterVersion>
                    <downloadJMeterDependencies>true</downloadJMeterDependencies>
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>benchmarks</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <!-- Forza il packaging jar durante questo profilo -->
                <packaging.type>jar</packaging.type>
            </properties>
            <build>
                <finalName>benchmarks</finalName>
                <plugins>
                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-shade-plugin</artifactId>
                        <version>3.5.1</version>
                        <executions>
                            <execution>
                                <phase>package</phase>
                                <goals>
                                    <goal>shade</goal>
                                </goals>
                                <configuration>
                                    <shadedArtifactAttached>true</shadedArtifactAttached>
                                    <shadedClassifierName>benchmarks</shadedClassifierName>
                                    <transformers>
                                        <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer">
                                            <mainClass>org.openjdk.jmh.Main</mainClass>
                                        </transformer>
                                    </transformers>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>
</project>